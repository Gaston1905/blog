import { SplitLiveArticlePipe } from './split-live-article.pipe';

describe('SplitLiveArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitLiveArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
